import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const ProviderBookingDetailsScreen = () => {
    return <Text style={{ fontSize: 25,flex:1,justifyContent:'center',textAlign:'center',marginTop:50 }}>ProviderBookingDetailsScreen</Text>
};

const style = StyleSheet.create({});

export default ProviderBookingDetailsScreen;